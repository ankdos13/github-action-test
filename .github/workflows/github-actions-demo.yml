on:
  pull_request:
    types:
      - closed
    branches:
      - preprod

jobs:
  preprod-deploy:
    run: echo ${{ github.ref}} 
    if: ${{ github.ref == 'preprod' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo The PR was merged
      - run: ls ${{ github.workspace }}